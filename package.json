{
  "name": "sotfest-upload",
  "private": true,
  "version": "1.0.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:major": "node scripts/incrementVersion.js major && git add package.json && npm run build",
    "build:minor": "node scripts/incrementVersion.js minor && git add package.json && npm run build",
    "build:patch": "node scripts/incrementVersion.js patch && git add package.json && npm run build",
    "lint": "eslint .",
    "preview": "vite preview",
    "predeploy": "git:checkdiff",
    "deploy:major": "npm run git:checkdiff && npm run build:major && npm run deploy:commit && npm run git:tag && npm run git:push && npm run deploy:gh",
    "deploy:minor": "npm run git:checkdiff && npm run build:minor && npm run deploy:commit && npm run git:tag && npm run git:push && npm run deploy:gh",
    "deploy:patch": "npm run git:checkdiff && npm run build:patch && npm run deploy:commit && npm run git:tag && npm run git:push && npm run deploy:gh",
    "deploy:commit": "node scripts/commitVersion.js",
    "deploy:gh": "gh-pages -d dist",
    "git:checkdiff": "git diff-index --quiet HEAD -- || (echo 'Error: You have uncommitted changes! Commit them first.' && exit 1)",
    "git:tag": "node scripts/createTag.js",
    "git:push": "git push origin HEAD && git push origin --tags"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^7.2.0",
    "@fortawesome/free-solid-svg-icons": "^7.2.0",
    "@fortawesome/react-fontawesome": "^3.2.0",
    "@supabase/supabase-js": "^2.103.0",
    "gh-pages": "^6.3.0",
    "react": "^19.2.4",
    "react-colorful": "^5.6.1",
    "react-dom": "^19.2.4",
    "react-sketch-canvas": "^7.0.0-next.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^24.12.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.0",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.56.1",
    "vite": "^8.0.0"
  }
}